<%- include('./partials/header') %>
  <table id="list">
    <thead>
      <tr>
        <th>Deck</th>
        <th>New</th>
        <th>Learn</th>
        <th>Due</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><a href="/decks/test">Zero to Mastery</a></td>
        <td>123</td>
        <td>86</td>
        <td>46</td>
      </tr>
      <tr>
        <td><a href="/decks/test">BigO Notation</a></td>
        <td>87</td>
        <td>12</td>
        <td>59</td>
      </tr>
      <% decks.forEach((d) => { %>  
        <tr>
          <td><a href="/decks/<%= d._id %>"><%= d.name %></a></td>
          <td>Tbd</td>
          <td>Tbd</td>
          <td>Tbd</td>
          <td>
            <% if (user?._id.equals(d.user)) { %>
              <form action="/decks/<%= d._id %>?_method=DELETE" method="POST">
                <input type="hidden" name="deck" value="<%= d._id %>">
                <button type="submit">X</button>
              </form>
            <% } %>
          </td>
        </tr>
      <% }) %>
      <tr>
        <td><a href="/decks/new">+ Add new Deck</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    </tbody>
  </table>
  <p>You've just reviewed 6 cards in 20 seconds.</p>
  <p>You've been studying non-stop for 5 days!</p>

  <%- include('./partials/footer') %>